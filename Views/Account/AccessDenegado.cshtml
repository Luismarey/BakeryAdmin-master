@{
    ViewData["Title"] = "Acceso Denegado";
    Layout = null; // Para usar el estilo tipo login
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Migas de Amor</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

    <style>
        body {
            background-color: #faf6f1;
            font-family: "Segoe UI", sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .denied-container {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .denied-card {
            background: #fffaf2;
            border: 2px solid #e6d3c1;
            border-radius: 16px;
            padding: 2.5rem;
            box-shadow: 0 4px 10px rgba(0,0,0,0.08);
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

            .denied-card h1 {
                font-family: "Georgia", serif;
                color: #6a3e1f;
                margin-bottom: 1rem;
            }

            .denied-card p {
                color: #6a3e1f;
                font-size: 1.1rem;
            }

        .btn-primary {
            background-color: #8b4a24;
            border-color: #8b4a24;
        }

            .btn-primary:hover {
                background-color: #6a3e1f;
                border-color: #6a3e1f;
            }

        .logo {
            display: block;
            margin: 0 auto 1rem;
            height: 90px;
        }

        footer {
            text-align: center;
            color: #8b4a24;
            font-size: 0.9rem;
            padding: 1rem;
            border-top: 1px solid #e6d3c1;
        }
    </style>
</head>
<body>
    <div class="denied-container">
        <div class="denied-card">
            <img src="/img/logo-horizontal.png" alt="Migas de Amor" class="logo" />
            <h1><i class="fa-solid fa-ban"></i> Acceso Denegado</h1>
            <p>No tienes permisos para acceder a esta página.</p>

            <a href="/" class="btn btn-primary mt-3"><i class="fa fa-home"></i> Volver al inicio</a>
        </div>
    </div>

    <footer>
        &copy; 2025 Migas de Amor — Pan fresco hecho con amor ❤
    </footer>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>